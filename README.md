# voxel-engine
A voxel engine written in OpenGL and C++.
Runs on both Windows and Linux.

## Cloning
```bash
git clone --recursive
```

## Building
```bash
mkdir build
cd build
cmake ..
cmake --build . -j 8
```
