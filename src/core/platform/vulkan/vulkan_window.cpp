#include "vulkan_window.hpp"

namespace Core::Graphics {
  VulkanWindow::VulkanWindow(const DisplayInfo &displayInfo, const WindowOptions &windowOptions) :
  Window(displayInfo, windowOptions) {

  }
}
