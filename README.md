# voxel-engine
A voxel engine written in OpenGL and C++.
Runs on both Windows and Linux.
Press P to enable wireframe mode and O to turn back off.

## Cloning
```bash
git clone --recursive
```

## Building
```bash
mkdir build
cd build
cmake ..
cmake --build . -j 8
```
